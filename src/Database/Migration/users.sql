CREATE TABLE users (id int auto_increment primary key,
                    name varchar(50),
                    email varchar(50),
                    password varchar(255),
                    role varchar(20) );